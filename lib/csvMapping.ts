export type ColumnMapping=Record<string,string|null>;
const aliases:Record<string,string[]>={name:["name","customer","customer name","full name"],email:["email","e-mail","email address"],phone:["phone","mobile","telephone"],company:["company","business","organization"],amount:["amount","revenue","price","total","value"],product:["product","item","product name"],sale_date:["sale date","date","transaction date"],category:["category","type","expense category"],expense_date:["expense date","date","transaction date"],description:["description","details","memo"]};
export function detectMapping(headers:string[]):ColumnMapping{const result:ColumnMapping={};for(const h of headers){const normalized=h.trim().toLowerCase();let match:string|null=null;for(const [field,names] of Object.entries(aliases)){if(names.includes(normalized)){match=field;break;}}result[h]=match;}return result;}
