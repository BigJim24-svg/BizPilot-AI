import { NextRequest, NextResponse } from "next/server";
import { supabaseAdmin, getBusinessIdForRequest } from "@/lib/supabase";
import { answerCopilotQuestion, runRecommendationAgent } from "@/lib/ai/agents";
import { temperatureFromScore } from "@/lib/scoring";
async function buildSnapshot(businessId:string){const [{data:sales},{data:expenses},{data:leads},{data:customers}]=await Promise.all([supabaseAdmin.from("sales").select("amount, product, sale_date").eq("business_id",businessId),supabaseAdmin.from("expenses").select("amount, expense_date").eq("business_id",businessId),supabaseAdmin.from("leads").select("score, value, customers(name)").eq("business_id",businessId).order("score",{ascending:false}).limit(10),supabaseAdmin.from("customers").select("name, lifetime_value, last_contact").eq("business_id",businessId)]);const now=Date.now();const daysSince=(d:string|null)=>d?Math.floor((now-new Date(d).getTime())/86400000):999;return {revenue:{current:sumRecent(sales,"amount",30),previous:sumRecent(sales,"amount",30,30)},expenses:{current:sumRecent(expenses,"amount",30),previous:sumRecent(expenses,"amount",30,30)},leads:(leads??[]).map((l:any)=>({name:l.customers?.name??"Unknown",score:l.score,value:l.value,reason:temperatureFromScore(l.score)})),customers:(customers??[]).map((c:any)=>({name:c.name,lifetimeValue:c.lifetime_value,inactiveDays:daysSince(c.last_contact)})),topProducts:[]};}
function sumRecent(rows:any[]|null,field:string,windowDays:number,offsetDays=0){if(!rows)return 0;const now=Date.now();const start=now-(offsetDays+windowDays)*86400000;const end=now-offsetDays*86400000;return rows.filter(r=>{const t=new Date(r.sale_date??r.expense_date).getTime();return t>=start&&t<end}).reduce((s,r)=>s+Number(r[field]??0),0);}
export async function POST(req:NextRequest){try{const token=req.headers.get("authorization")?.replace("Bearer ","")??"";const businessId=await getBusinessIdForRequest(token);if(!businessId)return NextResponse.json({error:"Unauthorized"},{status:401});const body=await req.json();if(typeof body.question!=="string"||!body.question.trim())return NextResponse.json({error:"question is required"},{status:400});const snapshot=await buildSnapshot(businessId);return NextResponse.json({answer:await answerCopilotQuestion(body.question.trim(),snapshot as any)});}catch(e:any){return NextResponse.json({error:e?.message||"AI request failed"},{status:500});}}
export async function GET(req:NextRequest){try{const token=req.headers.get("authorization")?.replace("Bearer ","")??"";const businessId=await getBusinessIdForRequest(token);if(!businessId)return NextResponse.json({error:"Unauthorized"},{status:401});const snapshot=await buildSnapshot(businessId);const result=await runRecommendationAgent(snapshot as any);await supabaseAdmin.from("ai_insights").insert({business_id:businessId,type:"synthesis",severity:"opportunity",title:"Daily business brief",description:result.synthesis,recommendation:result.synthesis});return NextResponse.json(result);}catch(e:any){return NextResponse.json({error:e?.message||"Recommendation request failed"},{status:500});}}
